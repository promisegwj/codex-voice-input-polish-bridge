# Changelog

本文档记录面向发布用户的重要变化。格式遵循“Unreleased + 版本条目”的简单结构。

## Unreleased

- 收紧持续学习候选规则：每日迭代会去重相同差异样本，跳过单字、短英文、URL、线程链接、文件名和整句级改写，只保留保守的精确替换候选。
- 扩展默认整理规则：结合当前文本纠正常见近音技术词，但禁止把文件名、链接、标题等一次性内容学成跨场景全局插入。
- 新增学习候选口癖 golden case，覆盖“让我一次，就是”这类无意义口述片段清理。
- 本地校准服务会在持续学习开启且到达每日迭代时间后处理前一天样本；手动“更新规则”会选择最近有样本的日期。
- 落地 v0.3 跨平台适配骨架：Windows 主链路保留，macOS 建立“读取语音历史 -> 本地整理 -> 审核页/剪贴板 -> 用户 Cmd+V”的最小闭环。
- 新增 macOS 平台分支：默认读取 `$HOME/.codex/transcription-history.jsonl`，剪贴板使用 `pbcopy` / `pbpaste`，自动粘贴默认关闭。
- 新增 `scripts/Publish-CodexVoiceBridge.ps1`，支持 `win-x64`、`osx-x64`、`osx-arm64` 和 `all` 发布目标。
- 新增 `/api/platform` 和平台能力声明，设置页增加 macOS 实验自动粘贴开关。
- 更新状态文案，明确区分“已回填”“已复制到剪贴板”“已跳过回填”，并提示用户确认后再发送。
- 新增 v0.3 跨平台设计与验收文档：`docs/cross-platform-macos-plan-v0.3.md`。
- 注意：本轮已完成 Windows 回归和跨平台发布产物生成；macOS 辅助功能权限和前台粘贴仍需实机验证。

## 0.2.0 - 2026-05-27

- 落地 v0.2.0 MVP：阶段 0、阶段 1 和最小回填状态闭环。
- 整理器新增 `--version` 和 `--debug-decision`，可输出结构决策摘要。
- 新增保守结构规划：默认不因整理规则里的“拆分、总结、要点、提示词”等词触发分条；只有明确枚举、明确步骤请求或高置信多任务信号才分条。
- 新增 `LiteralSpan` 保护和结构化输出校验，保护数字、端口、路径、版本号、声音参数、快捷键、否定、条件、疑问、不确定性、优先级、专有名词和近音技术词。
- 新增机器可读 golden case 与测试 runner：`tests/golden/voice-text-rule-cases.jsonl`、`tests/Run-VoiceBridgeGoldenCases.ps1`。
- 回填接口和设置页状态文案现在能区分“已回填到 Codex 输入框”“未回填，已复制到剪贴板”和“跳过自动粘贴原因”。
- 发布默认仍保持保守：不默认开启持续学习、自动应用、随 Codex 启动或云端 LLM/API。

## 0.1.0 - 2026-05-25

- 首个源代码发布准备版本。
- 支持读取 Codex 本地语音历史、剪贴板输入和网页审核流程。
- 支持本地候选规则生成，但不自动改写永久整理规则。
- 初始整理 Git 发布目录。
- 加入本地校准中心、语音文本整理器、持续学习样本记录和自动清理机制。
- 默认关闭持续学习、自动应用和随 Codex 启动。
- 将项目许可证改为 Apache-2.0。
